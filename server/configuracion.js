const db_user = 'root';
const db_password = '12345678';
const db_url = 'localhost';
const db_port = '3306';

module.exports = {
    user: db_user,
    password: db_password,
    url: db_url,
    port: db_port
}